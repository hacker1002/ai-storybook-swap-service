"""`/api/provenance/*` router group (P3c Gap 2). Read-only lookup of the reference
images sent to the AI provider in one past call, by `ai_service_logs.id`. Lets the
remix sub-app's Inpaint tab re-offer the previous generate's ref set."""
